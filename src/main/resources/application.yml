spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/complaintdb
    username: complaint_user
    password: complaint_pass

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

country:
  service:
    base:
      url: https://ipwho.is/
